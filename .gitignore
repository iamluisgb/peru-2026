node_modules/
.DS_Store
*.log

# Datos personales: NUNCA en el repo (ADR-002). Si algún día se exporta una copia local,
# que no se cuele por accidente.
data/personal*.json
*.privado.json

# Insumos del pipeline de fotos: se conservan porque documentan de dónde salió cada imagen.
# (No se ignoran; esta nota está para que quede claro que NO son datos de la app.)
